server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Serve .jsx to the in-browser Babel transpiler with a JS content type.
  types {
    text/html                             html htm;
    text/javascript                       js jsx mjs;
    text/css                              css;
    image/png                             png;
    image/svg+xml                         svg;
    application/json                      json webmanifest;
    image/x-icon                          ico;
  }
  default_type application/octet-stream;

  # HTML, config, service worker and env.js must never be cached — so a redeploy
  # is picked up on the next reload and the API URL is always current.
  location = / {
    add_header Cache-Control "no-store, no-cache, must-revalidate";
    try_files /index.html =404;
  }
  location ~* \.(html|json|webmanifest)$ {
    add_header Cache-Control "no-store, no-cache, must-revalidate";
  }
  location = /sw.js        { add_header Cache-Control "no-store"; }
  location = /config.js    { add_header Cache-Control "no-store"; }
  location = /env.js       { add_header Cache-Control "no-store"; }

  # JSX changes on every deploy in this in-browser-transpile setup — don't cache.
  location ~* \.jsx$       { add_header Cache-Control "no-store"; }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
